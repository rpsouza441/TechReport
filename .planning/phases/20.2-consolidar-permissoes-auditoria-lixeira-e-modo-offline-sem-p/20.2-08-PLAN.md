---
phase: 20.2-consolidar-permissoes-auditoria-lixeira-e-modo-offline-sem-p
plan: "08"
type: execute
wave: 4
depends_on: ["20.2-07"]
files_modified:
  - .gitattributes
  - supabase/operations/20_2_reset_allowlist.sql
  - supabase/operations/20_2_apply_reset.sh
  - supabase/operations/Stage-20_2MigrationReplay.ps1
  - supabase/operations/20_2_cleanup_storage.sh
  - supabase/operations/20_2_cleanup_auth.sh
  - supabase/tests/database/20_2_live_postflight.sql
autonomous: false
requirements: [R-05, R-06, NFR-01]
must_haves:
  truths:
    - "A recoverable full custom-format pg_dump, nonzero size, pg_restore catalog validation and SHA-256 exist under /srv/DATA/supabase before any mutation."
    - "Only the seven inventoried TechReport public tables, seventeen allowlisted current/target functions, TechReport storage policies, and TechReport migration-history versions may be reset; public, auth, storage and platform schemas/volumes remain intact."
    - "Storage bytes and bucket metadata are removed through the supported Storage API before SQL dependencies are removed; Auth users are removed later through the supported GoTrue Admin API, never by direct auth/storage DML."
    - "Every existing repository migration replays in the exact authoritative order 0001-0016, then 0018-0027; historical 0017 remains explicitly absent."
    - "Each staged migration and its history row commit atomically, and any reset, API, replay, history, postflight or pgTAP failure stops the sequence."
  artifacts:
    - path: supabase/operations/20_2_reset_allowlist.sql
      provides: "Transactional allowlisted TechReport schema reset with identity and Storage gates"
    - path: supabase/operations/20_2_apply_reset.sh
      provides: "Host-side backup/container/hash/pre-state gate and post-reset preservation proof"
    - path: supabase/operations/Stage-20_2MigrationReplay.ps1
      provides: "Deterministic 26-file atomic replay staging with source SHA-256 history"
    - path: supabase/operations/20_2_cleanup_storage.sh
      provides: "Supported Storage API empty/delete harness"
    - path: supabase/operations/20_2_cleanup_auth.sh
      provides: "Supported GoTrue Admin API user cleanup harness"
  key_links:
    - from: supabase/operations/Stage-20_2MigrationReplay.ps1
      to: supabase/migrations
      via: "exact filename manifest and per-source SHA-256"
      pattern: "0001-0016,0018-0027"
    - from: supabase/operations/20_2_reset_allowlist.sql
      to: supabase-db
      via: "database system identifier plus object allowlists"
      pattern: "expected_system_identifier"
---

<objective>
[BLOCKING] Reset only the development TechReport application surface while preserving the self-hosted Supabase platform, then replay every authoritative migration and validate the final security contract.

Purpose: Replace an untracked manual SQL baseline with a reproducible, traceable migration history without resetting Docker volumes, Studio credentials, or unrelated Supabase services.
Output: Verified full backup; supported Storage/Auth cleanup evidence; allowlisted reset output; ordered 26-migration replay manifest/history; postflight and pgTAP evidence.
</objective>

<execution_context>
@C:/Users/Rodrigo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Rodrigo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.2-consolidar-permissoes-auditoria-lixeira-e-modo-offline-sem-p/20.2-CONTEXT.md
@supabase/operations/20_2_reset_allowlist.sql
@supabase/operations/Stage-20_2MigrationReplay.ps1
@supabase/operations/20_2_cleanup_storage.sh
@supabase/operations/20_2_cleanup_auth.sh
@supabase/migrations/
@supabase/tests/database/20_2_live_postflight.sql
@supabase/tests/database/20_2_permissions_audit_trash.test.sql
</context>

<operator_facts>
- Read-only inventory completed with exit 0 against database `postgres` as role `postgres` in container `supabase-db`.
- Public inventory: exactly seven tables (`app_admins`, `empresas`, `rat_audit_log`, `rat_signature_attachments`, `rats`, `tecnico_convites`, `tecnicos`), thirteen current TechReport functions, and nineteen public plus four TechReport Storage policies.
- Data inventory: 8 companies, 13 technicians, 1 app admin, 15 invites, 70 RATs, 42 signature metadata rows, 14 Auth users (13 linked), bucket `rat-signatures` with 38 objects.
- No extension lives in `public`; no `public`/`storage` publication entry; no migration-history table exists.
- User explicitly authorized destructive reset because this installation is development-only and contains no important data. Studio login, platform services, configuration and Docker volumes must remain intact.
</operator_facts>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: [BLOCKING] Create and prove recoverable full database backup</name>
  <files>none (remote backup checkpoint)</files>
  <action>From `C:\ws\TechReport`, run only the single PowerShell here-string pipeline supplied in the checkpoint response. It SSHes with `C:\Users\Rodrigo\.ssh\codexanalysis` to `codexanalysis@192.168.22.245`, enters one `sudo -n bash -s` session, asserts `supabase-db` is running and has a Supabase Postgres image, captures the exact container ID and PostgreSQL system identifier, writes a custom-format full `pg_dump` under `/srv/DATA/supabase/backups/techreport-reset`, checks nonzero size, validates the archive catalog with `pg_restore --list`, writes a SHA-256 sidecar, and prints only nonsensitive evidence. Do not run Storage/Auth/reset/replay commands yet.</action>
  <verify>
    <manual>Require exit 0 plus `BACKUP_GATE=PASS`, absolute backup path, nonzero bytes, SHA-256, container ID, system identifier, and successful `pg_restore --list`. Retain those exact identity/hash values for every later destructive command.</manual>
  </verify>
  <acceptance_criteria>A full recoverable backup exists outside the container under `/srv/DATA/supabase`, with integrity and database/container identity evidence.</acceptance_criteria>
  <done>Backup gate evidence is complete and no server data was mutated.</done>
  <resume-signal>Paste the complete backup command output and exit code.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Empty and remove only rat-signatures through Storage API</name>
  <files>supabase/operations/20_2_cleanup_storage.sh</files>
  <action>After Task 1 passes, pipe `20_2_cleanup_storage.sh` to the host under `sudo -n` with credential source `container-env:supabase-storage:SERVICE_KEY`. The harness must require the running `supabase-storage` container with a Supabase Storage API image and retrieve exactly one nonempty, exact-name `SERVICE_KEY` from its configured environment without printing the value or placing it in process arguments. Pass API base `http://127.0.0.1:18000`, the exact Task 1 container ID/system identifier, and expected object count `38`. The harness must assert those identities/counts, call the supported Storage API to empty then delete only `rat-signatures`, and verify both `storage.objects` count zero and bucket absence. Never delete from storage tables or filesystem paths directly.</action>
  <verify>
    <manual>Require exit 0 and `TECHREPORT_STORAGE_API_CLEANUP=PASS objects_removed=38 bucket=rat-signatures`; re-run a read-only bucket/object inventory.</manual>
  </verify>
  <acceptance_criteria>Storage service and metadata agree that only the TechReport bucket is absent; unrelated buckets/services remain unchanged.</acceptance_criteria>
  <done>The Storage dependency is safely removed before the public schema reset.</done>
  <resume-signal>Paste complete Storage harness output, exit code, and read-only verification.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Apply transactional allowlisted public reset</name>
  <files>supabase/operations/20_2_apply_reset.sh, supabase/operations/20_2_reset_allowlist.sql</files>
  <action>Copy both reset artifacts to a private temporary host directory, verify their local SHA-256 values after transport, then invoke `20_2_apply_reset.sh` under `sudo -n` with the exact backup path/hash, Task 1 container ID/system identifier and reset SQL hash. The wrapper revalidates the backup archive, container/database identity, exact post-Storage pre-state counts/catalog, public ownership by `supabase_admin`, Storage relation ownership by `supabase_storage_admin`, internal-schema and extension fingerprints, then proves a direct local psql connection has `current_user=session_user=supabase_admin`, `rolsuper=true`, and the expected system identifier. Only that direct owner/superuser connection may execute the single transaction, allowing it to remove the four policies on `storage.objects` and the exactly-owned public surface atomically without `SET ROLE`. The script has no `DROP SCHEMA`, no `CASCADE`, no Auth DML, no Storage table DML, and no Docker operation. It must commit once or roll back entirely, bootstrap only `supabase_migrations.schema_migrations(version text, statements text[], name text)`, and clear only the 26 allowlisted TechReport history versions.</action>
  <verify>
    <manual>Require exit 0 plus `TECHREPORT_RESET_HOST_PRECHECK=PASS`, `TECHREPORT_RESET_DIRECT_PRINCIPAL=PASS`, `TECHREPORT_RESET_LIVE_PRESTATE=PASS`, `TECHREPORT_RESET_OWNER_GATE=PASS`, `TECHREPORT_RESET_PRESTATE=PASS`, `TECHREPORT_RESET_ACTIVE_ROLE=PASS`, `TECHREPORT_RESET_SESSION_ROLE_UNCHANGED=PASS`, `TECHREPORT_ALLOWLIST_RESET=PASS`, `TECHREPORT_RESET_PRESERVATION=PASS`, and `TECHREPORT_RESET_TASK3=PASS`. Read-only verification must show zero public relations/functions, no TechReport storage policies, Auth users still 14, platform schemas/extensions unchanged, and the empty three-column history table.</manual>
  </verify>
  <acceptance_criteria>Only the TechReport SQL surface is absent; Auth, Storage, Studio, extensions, configuration and volumes are preserved.</acceptance_criteria>
  <done>Allowlisted reset is atomically complete.</done>
  <resume-signal>Paste complete reset and verification outputs with exit codes.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: Remove Auth users through GoTrue Admin API</name>
  <files>supabase/operations/20_2_cleanup_auth.sh</files>
  <action>Only after the public FK surface is absent, copy `20_2_cleanup_auth.sh` to the host, verify its transported SHA-256, and invoke it under `sudo -n`. Pass the already-proven `container-env:supabase-storage:SERVICE_KEY` credential selector, API base `http://127.0.0.1:18000`, exact Task 1 backup/container/system identities, and expected user count `14`; no `.env` is required. Before mutation, the harness revalidates the backup and exact Task 3 post-state, checks the database/Auth/credential containers, proves the Admin API UUID set exactly equals `auth.users`, and snapshots the complete Auth schema plus all non-user table data. It deletes only those 14 captured UUIDs through `DELETE /auth/v1/admin/users/{id}`, then requires API users, `auth.users`, identities, sessions, and refresh tokens to be zero while Auth schema/non-user data, platform schemas, extensions, Studio credentials, and service configuration remain unchanged. It never prints the service-role key and never performs direct Auth DML.</action>
  <verify>
    <manual>Require exit 0 plus `CREDENTIAL_SOURCE=PASS`, `BACKUP_RECHECK=PASS`, `AUTH_RESET_POSTSTATE=PASS`, `GOTRUE_PRECHECK=PASS`, `AUTH_CORE_CLEANUP=PASS users=0 identities=0 sessions=0 refresh_tokens=0`, `AUTH_PLATFORM_PRESERVATION=PASS`, and `TECHREPORT_GOTRUE_ADMIN_CLEANUP=PASS users_removed=14`; confirm Studio remains reachable with its existing login.</manual>
  </verify>
  <acceptance_criteria>GoTrue owns Auth cleanup and no application/platform credential or container configuration is reset.</acceptance_criteria>
  <done>Fresh Auth state exists for the new company bootstrap.</done>
  <resume-signal>Paste complete GoTrue harness output, exit code, zero-count verification, and Studio reachability result.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 5: Stage and replay all 26 authoritative migrations atomically</name>
  <files>supabase/operations/Stage-20_2MigrationReplay.ps1, supabase/migrations/*.sql</files>
  <action>Run `Stage-20_2MigrationReplay.ps1` into a new empty local temporary directory. It must find exactly the 26 repository files, refuse unexpected/missing files, prove historical 0017 absent, preserve each migration body, remove only its optional outer transaction wrapper, add one transaction plus same-transaction history insert containing the source SHA-256, and emit `replay-manifest.json`. Review the manifest, then pipe each staged SQL file individually and strictly in order to the identity-checked `supabase-db` psql command with `ON_ERROR_STOP=1`. Stop on the first nonzero exit; never use `ON CONFLICT` for history, never synthesize 0017, never edit an authoritative migration, and never continue after partial failure. Expected order: 0001-0016, 0018-0027.</action>
  <verify>
    <manual>Require 26 ordered `MIGRATION_REPLAYED` markers and exit 0 for every file. History must contain exactly those 26 versions once each, source hashes must match the local manifest, 0017 must be absent, and no other version may appear.</manual>
  </verify>
  <acceptance_criteria>Every existing migration is applied exactly once with atomic history, including originally unenveloped 0008, 0015, 0016 and 0021-0023.</acceptance_criteria>
  <done>Authoritative schema and traceable history are rebuilt from zero.</done>
  <resume-signal>Paste the staging output/manifest hash, all ordered replay outputs and exit codes, and final history query.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 6: Validate postflight, pgTAP, services, and fresh-company readiness</name>
  <files>supabase/tests/database/20_2_live_postflight.sql, supabase/tests/database/20_2_permissions_audit_trash.test.sql</files>
  <action>Run the live postflight then the complete pgTAP role/company/audit/trash matrix against the identity-checked database. Stop on any SQL error or failed assertion. Verify fixtures roll back. Re-run the inventory: seven expected public tables; final expected functions/policies/triggers; zero application data/Auth users; private `rat-signatures` bucket recreated by 0015 with zero objects; 26 ordered history rows; platform containers and Studio login preserved. Do not create the fresh company until all checks pass; fresh company/bootstrap is the next explicit operator action.</action>
  <verify>
    <manual>Require postflight exit 0, zero failed pgTAP tests, no residue, healthy Auth/Storage/PostgREST/Studio, and inventory matching the repository end state.</manual>
  </verify>
  <acceptance_criteria>The rebuilt development installation proves D-01-D-10 and D-13-D-15 and is ready for a new company without retaining prior application data.</acceptance_criteria>
  <done>[BLOCKING] Reset/replay and validation evidence is complete; downstream client plans may begin.</done>
  <resume-signal>Paste all verification outputs and exit codes, then confirm whether to bootstrap the fresh company.</resume-signal>
</task>

</tasks>

<threat_model>
## Assets

| Asset | Protection objective |
|---|---|
| Self-hosted Supabase platform/Studio login | Preserve services, configuration, schemas and volumes |
| Recoverability | Full validated pg_dump plus SHA-256 before mutation |
| TechReport schema/history | Exact allowlisted reset and authoritative ordered replay |
| Auth and Storage consistency | Mutate through their supported service APIs only |

## Trust Boundaries

| Boundary | Description |
|---|---|
| Local reviewed harness -> SSH -> sudo -> Docker | Destructive actions require exact container and PostgreSQL system identities |
| Storage/GoTrue APIs -> platform metadata | Services own cleanup of objects, buckets and users |
| Authoritative migration bytes -> staged transaction -> psql | Only transaction wrappers/history are added; source hashes remain traceable |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Concrete mitigation/test |
|---|---|---:|---|---|---|
| T-20.2-24 | Tampering/DoS | CRITICAL | reset target | mitigate | Exact container/system ID, relation/function/policy allowlists, no DROP SCHEMA/CASCADE/volume reset |
| T-20.2-25 | Data loss | CRITICAL | database | mitigate | Full custom pg_dump, nonzero check, pg_restore list validation, SHA-256 sidecar |
| T-20.2-26 | Inconsistency | HIGH | Storage/Auth | mitigate | Supported APIs only; before/after DB/API counts; ordered Storage -> public -> Auth sequence |
| T-20.2-27 | Tampering/Repudiation | HIGH | migration replay | mitigate | Exact 26-file manifest, explicit absent 0017, SHA-256 per source, same-transaction history |
| T-20.2-28 | Elevation/Disclosure | HIGH | deployed RLS/functions | mitigate | Postflight plus full role/company pgTAP and secret-free outputs |
</threat_model>

## Artifacts this phase produces

- Recoverable full-database backup outside the container (operational artifact, never committed).
- Five narrowly scoped local operation harnesses listed in frontmatter.
- Ordered command evidence and `20.2-08-SUMMARY.md` only after all six gates pass.

<source_coverage>
REQ R-05/R-06/NFR-01; user-authorized development reset; repository migrations 0001-0016 and 0018-0027; CONTEXT D-01-D-15; Storage/Auth supported API ownership. Status: COVERED.
</source_coverage>

<verification>
Completion requires remote backup, service-owned cleanup, transactional reset/replay, exact history and live behavioral evidence. Local static checks alone are insufficient.
</verification>

<success_criteria>
- Full backup is recoverable and integrity-checked before mutation.
- No platform schema, Docker volume, Studio credential, extension, publication or unrelated service object is reset.
- Storage and Auth cleanup occur only through supported APIs in dependency-safe order.
- Exactly 0001-0016 and 0018-0027 replay with source hashes and atomic history; 0017 remains absent.
- Postflight, pgTAP, service health and final zero-data inventory are green.
</success_criteria>

<output>
Create `.planning/phases/20.2-consolidar-permissoes-auditoria-lixeira-e-modo-offline-sem-p/20.2-08-SUMMARY.md` only after every checkpoint and verification passes.
</output>
