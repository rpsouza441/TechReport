---
wave: 1
depends_on: []
requirements_addressed:
  - PERF-01
  - PERF-02
  - PERF-03
files_modified:
  - lib/app/navigation/company_shell.dart
  - lib/features/company_admin/presentation/screens/admin_empresa_area.dart
  - lib/features/rat/presentation/screens/rat_list_screen.dart
  - lib/features/rat/presentation/view_models/rat_form_view_model.dart
autonomous: true
---

# Plan 01: Performance Improvements

## Objective

Melhorar performance do app corrigindo rebuilds desnecessários, implementando paginação na lista RATs, e corrigindo o gerador de números de RAT.

## Tasks

### Task 1: company_shell.dart - Selector para rebuild granular (PERF-01)

**Action:**
1. Ler arquivo `lib/app/navigation/company_shell.dart`
2. Localizar `AnimatedBuilder` na linha 250
3. Substituir por `Selector<ViewModel, T>` para cada propriedade que precisa de rebuild
4. Manter `AnimatedBuilder` apenas para animações reais (se houver)

**Files to modify:**
- `lib/app/navigation/company_shell.dart`

**Acceptance criteria:**
- Rebuild apenas dos widgets que dependem de propriedades específicas
- App continua funcionando normalmente

---

### Task 2: admin_empresa_area.dart - Selector para rebuild granular (PERF-01)

**Action:**
1. Ler arquivo `lib/features/company_admin/presentation/screens/admin_empresa_area.dart`
2. Localizar `AnimatedBuilder` nas linhas 42-73
3. Substituir por `Selector<AdminEmpresaViewModel, T>` para propriedades específicas
4. Manter rebuild granular para `isLoading`, `errorMessage`, lista deconvites

**Files to modify:**
- `lib/features/company_admin/presentation/screens/admin_empresa_area.dart`

**Acceptance criteria:**
- Rebuild apenas dos widgets que dependem de propriedades específicas
- ViewModel continua funcionando

---

### Task 3: rat_list_screen.dart - ListView.builder com paginação (PERF-02)

**Action:**
1. Ler arquivo `lib/features/rat/presentation/screens/rat_list_screen.dart`
2. Localizar `ListView.separated` nas linhas 195-228
3. Substituir por `ListView.builder` para virtualização
4. Implementar paginação:
   - Carregar `pageSize` items iniciais
   - Detectar scroll próximo do fim
   - Carregar próxima página
5. Manter filtros e ordenação

**Files to modify:**
- `lib/features/rat/presentation/screens/rat_list_screen.dart`

**Acceptance criteria:**
- Virtualização ativa para listas grandes
- Paginação funcional
- Scroll suave com muitos RATs

---

### Task 4: rat_form_view_model.dart - UUID para número de RAT (PERF-03)

**Action:**
1. Ler arquivo `lib/features/rat/presentation/view_models/rat_form_view_model.dart`
2. Localizar `_newRatNumber()` nas linhas 811-813
3. Substituir por UUID v4
4. Manter `ratId` como UUID já existente
5. O campo `numero` pode manter timestamp + UUID parcial para legibilidade

**Files to modify:**
- `lib/features/rat/presentation/view_models/rat_form_view_model.dart`

**Acceptance criteria:**
- UUID garante unicidade mesmo em alta velocidade
- Formato legível mantido
- Sem breaking changes

---

## Verification

- [ ] Selector substitui AnimatedBuilder nos arquivos
- [ ] ListView.builder com paginação funcional
- [ ] UUID gera números únicos
- [ ] App compila sem erros
- [ ] Testes existentes passam

## Artifacts this phase produces

- Widgets com rebuild granular
- Lista RATs com paginação
- Número de RAT usando UUID

---

*Plan: 09.9-01-performance*
*Created: 2026-06-15*
